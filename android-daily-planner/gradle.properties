# AndroidX configuration required by Android Gradle Plugin when using androidx.* deps
android.useAndroidX=true

# Keep Jetifier enabled for maximum dependency compatibility in sample projects
android.enableJetifier=true

# Optional JVM tuning for Gradle daemon in CI/local
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
